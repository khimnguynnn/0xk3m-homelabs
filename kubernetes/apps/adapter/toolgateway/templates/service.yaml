apiVersion: v1
kind: Service
metadata:
  name: toolgateway-service
  namespace: {{ .Release.Namespace }}
spec:
  clusterIP: None
  selector:
    adapter/type: mcp
  ports:
    - name: http
      port: 8000
      targetPort: 8000
